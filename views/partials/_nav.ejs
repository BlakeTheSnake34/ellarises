<nav class="er-nav">

  <!-- LEFT: Brand -->
  <div class="nav-left no-wrap">
    <a href="/" class="nav-logo">
      <img src="/images/ella-logo.png" class="nav-logo-img" alt="Ella Rises Logo">
      Ella Rises
    </a>
  </div>

  <!-- MIDDLE -->
  <div class="nav-middle no-wrap">
    <% if (currentUser) { %>

      <a href="/home" class="no-wrap">
        <span class="nav-icon">🏠</span> Home
      </a>

      <a href="/events" class="no-wrap">
        <span class="nav-icon">📅</span> Events
      </a>

      <% if (currentUser.role !== 'manager') { %>
        <a href="/surveys/new" class="no-wrap">
          <span class="nav-icon">📝</span> Take Survey
        </a>
      <% } %>

      <a href="/my-donations" class="no-wrap">
        <span class="nav-icon">💖</span> My Donations
      </a>

      <% if (currentUser.role === 'manager') { %>

        <!-- MANAGER DROPDOWN -->
        <div class="nav-dropdown no-wrap">
          <button class="nav-dropdown-btn">
            <span class="nav-icon">⚙️</span> Manager Menu ▾
          </button>

          <div class="nav-dropdown-content">
            <a href="/dashboard"><span class="nav-icon">📊</span> Dashboard</a>
            <a href="/participants"><span class="nav-icon">👥</span> Participants</a>
            <a href="/milestones"><span class="nav-icon">🎓</span> Milestones</a>
            <a href="/surveys"><span class="nav-icon">📈</span> Survey Results</a>
            <a href="/donations"><span class="nav-icon">💰</span> Donations</a>
            <a href="/admin/make-manager"><span class="nav-icon">🔑</span> Manager Access</a>
          </div>
        </div>

      <% } %>
    <% } %>
  </div>

  <!-- RIGHT -->
  <div class="nav-right no-wrap">
    <% if (currentUser) { %>
      <form action="/logout" method="POST" style="display:inline;">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <button type="submit" class="button-link">Logout</button>
      </form>
    <% } else { %>
      <a href="/login" class="button-link">Login</a>
      <a href="/signup" class="button-link button-secondary">Sign Up</a>
    <% } %>
  </div>

</nav>







