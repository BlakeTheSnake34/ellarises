<nav class="er-nav">

  <!-- LEFT: Brand -->
  <div class="nav-left">
    <a href="/" class="nav-logo">
      <img src="/images/ella-logo.png" class="nav-logo-img" alt="Ella Rises Logo">
      Ella Rises
    </a>
  </div>

  <!-- MIDDLE NAV LINKS -->
  <div class="nav-middle">
    <% if (currentUser) { %>

      <!-- Participant / General Navigation -->
      <a href="/home">
        <span class="nav-icon">🏠</span>
        <span>Home</span>
      </a>

      <a href="/events">
        <span class="nav-icon">📅</span>
        <span>Events</span>
      </a>

      <% if (currentUser.role !== 'manager') { %>
        <a href="/surveys/new">
          <span class="nav-icon">📝</span>
          <span>Take Survey</span>
        </a>
      <% } %>
      
      <a href="/my-donations">
        <span class="nav-icon">💖</span>
        <span>My Donations</span>
      </a>


      <% if (currentUser.role === 'manager') { %>
        <span class="nav-divider"></span>

        <!-- MANAGER DASHBOARD -->
        <a href="/dashboard" class="nav-manager-link">
          <span class="nav-icon">📊</span>
          <span>Dashboard</span>
        </a>

        <!-- DROPDOWN FOR MANAGER TOOLS -->
        <div class="nav-dropdown nav-manager-link">
          <button type="button" class="nav-dropdown-toggle">
            <span class="nav-icon">🔧</span>
            <span>Admin Tools</span>
            <span class="nav-caret">▾</span>
          </button>

          <div class="nav-dropdown-menu">

            <a href="/participants">
              <span class="nav-icon">👥</span>
              <span>Participants</span>
            </a>

            <a href="/milestones">
              <span class="nav-icon">🎓</span>
              <span>Milestones</span>
            </a>

            <a href="/surveys">
              <span class="nav-icon">📈</span>
              <span>Survey Results</span>
            </a>

            <a href="/donations">
              <span class="nav-icon">💰</span>
              <span>Donations</span>
            </a>

            <a href="/admin/make-manager">
              <span class="nav-icon">🔑</span>
              <span>Manager Access</span>
            </a>

          </div>
        </div>
      <% } %>

    <% } %>
  </div>

  <!-- RIGHT: Auth -->
  <div class="nav-right">
    <% if (currentUser) { %>
      <form action="/logout" method="POST" style="display:inline;">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <button type="submit" class="button-link">Logout</button>
      </form>
    <% } else { %>
      <a href="/login" class="button-link">Login</a>
      <a href="/signup" class="button-link button-secondary">Sign Up</a>
    <% } %>
  </div>

</nav>




